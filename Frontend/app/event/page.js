import EventAndDining from '../../components/EventAndDining';

export const metadata = {
  title: 'Events & Dining — Hotel Management',
};

export default function EventPage() {
  return <EventAndDining />;
}
